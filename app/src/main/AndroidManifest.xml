<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.ddnosh.arabbit.sample">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splash.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".network.NetworkActivity" />
        <activity android:name=".image.GlideActivity" />
        <activity android:name=".base.FrameActivity"  />
        <activity android:name=".coroutine.CoroutineActivity" />
        <activity android:name=".dialog.DialogActivity" />
        <activity android:name=".mvvm.LoginActivity" />
        <activity android:name=".livedata.LiveDataActivity" />
        <activity android:name="com.github.ddnosh.arabbit.sample.binding.viewbinding.VBActivity" />
        <activity android:name=".JavaTestActivity" />
        <activity android:name=".binding.databinding.DBActivity" />
    </application>

</manifest>